paths:
  data_root: "data"

run:
  league: "NHL"
  slate_id: "DAILY"
  timezone: "UTC"
  run_date: ""   # leave blank for today (UTC). Or set like "2025-01-15"

quality_gates:
  min_matchups: 1
  max_missing_critical_pct: 0.25

risk:
  min_confidence: 0.10
  max_uncertainty: 0.95